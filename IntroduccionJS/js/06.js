// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5); // 3
resultado = Math.ceil(2.1);  // 3
resultado = Math.floor(2.9);  // 2
resultado = Math.sqrt(144); // 12
resultado = Math.abs(-200); // 200
resultado = Math.min(3, 4, 5, 1, 2, 6, 2, 1, -1);
resultado = Math.max(3, 4, 5, 1, 2, 6, 2, 1, -1);
resultado = Math.random();
resultado = Math.floor(Math.random() * 100);


console.log(resultado);